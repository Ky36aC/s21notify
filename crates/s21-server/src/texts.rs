//! Тексты бота (команды — порт bot.py v2.1).

pub const HELP_TEXT: &str = "<b>Команды:</b>\n\
/reviews — ближайшие проверки\n\
/agenda — события на 7 дней\n\
/deadlines — активные дедлайны\n\
/status — уровень, XP, коины\n\
/check — проверить прямо сейчас\n\
/help — эта справка";

pub const MINIAPP_BUTTON: &str = "⚙️ Открыть настройки";

pub fn welcome_registered(login: &str) -> String {
    format!(
        "✅ <b>С возвращением, {login}!</b>\nУведомления платформы снова идут в этот чат.\n\n{HELP_TEXT}"
    )
}

pub fn welcome_new() -> String {
    "👋 <b>Привет! Я — s21notify.</b>\n\
     Пересылаю события платформы Школы 21: записи на проверку, напоминания \
     с будильником, дедлайны и ленту.\n\n\
     Чтобы подключить свой аккаунт, открой настройки кнопкой ниже и войди \
     логином платформы. Пароль не сохраняется — он нужен один раз для входа."
        .to_string()
}

pub fn relogin_needed() -> String {
    "⚠️ <b>Платформа отозвала доступ</b> (обычно после смены пароля).\n\
     Уведомления приостановлены. Открой настройки и войди заново."
        .to_string()
}

pub const ACK_TOAST: &str = "Принято! Хорошей проверки 🍪";

pub fn check_started() -> String {
    "🔄 Проверяю прямо сейчас — если есть что-то новое, пришлю отдельным сообщением.".to_string()
}

pub fn not_registered() -> String {
    "Ты ещё не подключил аккаунт платформы — открой настройки кнопкой ниже.".to_string()
}
